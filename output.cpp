#include <cstdint>
#include <chrono>
#include "cpp2util.h"
int main() { using std::chrono_literals::_ ;
  assert( 10 as i32 == 10 );
  assert( 10LL as i32 == 10 );
  assert( 10s as std::chrono::seconds == 10s ); }
