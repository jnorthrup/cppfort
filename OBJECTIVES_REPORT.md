# Project Objectives Report: Main Branch

This report outlines the current objectives, status, and priorities for the `main` branch of the project, based on repository documentation and codebase analysis.

## 🎯 Primary Objective: Full Corpus Transpile Validation

**Track Status:** `ACTIVE`
**Goal:** Achieve **100% transpile accuracy** for the entire `cppfront` regression corpus, ensuring the output matches the reference `cppfront` implementation.

### Scope
- **Target:** All **191** `.cpp2` files in `third_party/cppfront/regression-tests`.
- **Constraint:** Output must match `cppfront` reference output (isomorphic checks).
- **Metric:** Semantic loss < 0.05 average (secondary), but binary match/compile success is primary.

### Current Status
- **Progress:** Approximately **71%** (134/189 files passing as of last update).
- **Recent Wins:**
  - Loop initializer syntax (`(copy i:=0)`).
  - Template argument capture fixes.
  - Parser improvements (compile-time functions, `@flag_enum`, `concept` keyword).

### 🚧 Immediate Blockers
The following features are identified as critical blockers preventing 100% compliance:
1.  **Variadics:** Handling `Ts...` parameter packs (~10 files affected).
2.  **Type & Namespace Aliases:** Correct parsing and emission (~8 files affected).
3.  **`inspect` / `is` patterns:** Complex pattern matching expressions (~10+ files affected).
4.  **Unary `+` operator:** Operator overloading specifics.
5.  **UFCS Edge Cases:** Uniform Function Call Syntax resolution in complex contexts.

---

## 📅 Roadmap & Secondary Objectives

### 1. Semantic AST Enhancements (Planned)
**Status:** `PLANNED` (15-18 day roadmap)
**Goal:** Enhance the AST to support advanced semantic analysis.
- Escape analysis (value lifetimes).
- Borrowing/ownership tracking (Rust-like semantics).
- External memory pipeline integration.
- Channelized concurrency.

### 2. Regression Corpus Semantic Preservation (Blocked)
**Status:** `BLOCKED`
**Goal:** Ensure transpilation preserves semantic correctness beyond just syntax.
- **Blockers:**
  - Parameter semantics (inout being passed by-value instead of reference).
  - Mixed-mode C++1 syntax support.

---

## ✅ Completed Objectives
The following tracks have been successfully completed and integrated:
1.  **Clang AST → MLIR Mapping Infrastructure:** Tools to map C++ AST nodes to MLIR operations (`cpp2` dialect).
2.  **SCCP Pass:** Sparse Conditional Constant Propagation optimization pass.
3.  **Markdown CAS:** Content-Addressable Storage for module stubs using Markdown comments.

---

## 🛠 verification Instructions
To verify the current state of the primary objective:
1.  **Build the compiler:** `cmake -B build && cmake --build build`
2.  **Run the regression suite:** `./build/regression_suite`
    - *Note:* The full suite runs 191 tests and may take significant time.
    - Output will list `OK` or `FAIL` for Transpile, Source Match, Compile, and Execution Match.

Generated by: Jules (Agent)
Date: 2025-01-01
