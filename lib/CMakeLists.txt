# lib/CMakeLists.txt
# SoN dialect and passes — source files not yet committed.
# Stubs so cmake configure succeeds without MLIR.

add_library(Cpp2SONDialect INTERFACE)
add_library(Cpp2SONPasses INTERFACE)
