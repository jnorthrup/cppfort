#include <iostream>
#include <vector>
#include <vector>

template<auto>
auto f(auto, auto) -> void { }

constexpr int a = 1;

int main() { auto p = new<int>(11);
    std::cout << p * << "\n";

    std::vector<int> v = ( 1, 2, 3 );
    std::cout << (1+2)*(3+v[0]);
    std::cout << "\n13*14 is (13*14)$\n";
    f<(1>2)>(3,4);
    f< a+a >(5,6); }
