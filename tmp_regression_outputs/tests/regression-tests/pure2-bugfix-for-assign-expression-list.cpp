#include <vector>
int main() { using vec = = std::vector<int>;
  vec v = (0);
  v                   = ();
  assert( v == :vec = () );
  v                   = (1);
  assert( v == :vec = (1) );
  v                   = (2, 3);
  assert( v == :vec = (2, 3) ); }
