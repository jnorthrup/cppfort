#include <iostream>
#include <vector>

int main() { std::vector<int> v = (1, 2, 3, 4, 5);
    auto counter = 42;
    for v  next counter *= 2  do (i) {
        std::cout << i << " " << counter << "\n";
    } }

#include <span>
