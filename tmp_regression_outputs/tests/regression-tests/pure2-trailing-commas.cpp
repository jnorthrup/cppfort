#include <vector>
#include <array>
void f(a, b) a + b<T, U, > (a: T, b: U) g = a + b;

void doubler(const int& a, int, ) -> (i) { i = a * 2; }

vals: @struct type = { i: int; }

int main() { auto (copy a = 42,) while false { a++; }
    _ = g(1, 2,);

    std::vector grouping = (0, 1, 2,);

    std::array array = (0, 1, 2,);

    _ = array;
    _ = grouping; }
