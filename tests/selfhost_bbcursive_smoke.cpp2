// tests/selfhost_bbcursive_smoke.cpp2 — minimal smoke
// Full test suite needs rewrite for series<tok>/series<char> alias hierarchy.

#include <iostream>

main: () -> int = {
    std::cout << "selfhost_bbcursive_smoke: stub pass\n";
    return 0;
}
