#include "cpp2util.h"

auto print_and_decorate(auto x) -> void;

auto main() -> int {
    int x = 42;
    *int p = default;
if (std::rand() ?op? 2)     {
    }
 else     {
    }
    print_and_decorate(*p);
    print_and_decorate(x);
}

auto print_and_decorate(auto x) -> void {
    std::cout << ">> " << x << "\n";
}

