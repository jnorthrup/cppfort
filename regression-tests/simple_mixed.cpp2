#include <iostream>
#include <string>

decorate: (inout s: std::string) = {
    s = "[" + s + "]";
}

name: () -> std::string = {
    s: std::string = "world";
    decorate(s);
    return s;
}

auto main() -> int {
    std::cout << "Hello " << name() << "\n";
}