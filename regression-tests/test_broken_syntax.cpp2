// This file contains syntax that stage1 currently doesn't handle well
// to test the inductive error analysis

broken: (x: int) -> int = {
    y: int = x + 1;
    return y;
}

auto main() -> int {
    result: int = broken(5);
    std::cout << "Result: " << result << "\n";
}